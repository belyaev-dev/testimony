apiVersion: v1
kind: Service
metadata:
  name: {{ include "testimony.fullname" . }}
  labels:
    {{- include "testimony.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "testimony.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
